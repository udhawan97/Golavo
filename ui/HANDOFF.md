# Phase-0 UI Handoff — Forecast Audit Workbench

**Lane:** Claude (owns `ui/**` only).
**BASE_SHA:** `211637a` (`origin/main` at start; matched the expected last-known SHA).
**Branch:** `friendly-helper-claude/phase-0-audit-workbench-70fe73` — a pre-provisioned
worktree branch cut from `origin/main`. The brief asked for `lane/claude-ui`; the
session was already checked out on this isolated branch off the same base (the main
checkout holds `lane/codex-core`), so I built here rather than add a second branch to
the worktree. Same effect: an isolated branch off BASE_SHA. Rebase on `main` after the
Codex lane merges, then merge the PR.

## What this is

A beautiful, honest, **read-only** UI over sealed forecast artifacts, driven entirely
by mock JSON conforming to frozen contract **v0.1.0**. No live integrations, no invented
endpoints, no backend changes. Not a betting product — no odds framing anywhere.

## Acceptance checks

| Check | Result |
| --- | --- |
| `npm install` | clean (71 pkgs, 0 vuln) |
| `npm run build` (`tsc --noEmit && vite build`) | clean |
| `tsc --noEmit` | clean |
| Runs at `127.0.0.1:5173` with mocks by default (no `VITE_GOLAVO_API`) | yes¹ |
| All four views reachable, every state renders from mocks alone | yes |
| Initial JS ≤ 300 KB gzip | **~56 KB gzip** (mocks lazy-loaded as ~1 KB chunks; CSS 4.4 KB gzip) |
| No heavy chart libs — reliability diagram is hand-rolled SVG | yes (deps: react + react-dom only) |
| Keyboard nav + visible focus + reduced-motion | verified (see below) |
| No betting lexicon (odds/lock/value/units/pick) in diff | verified via grep |
| `git diff --name-only` touches `ui/**` only | yes |

¹ Verified on `127.0.0.1:5174` because port 5173 was held by a parallel session's
server. The committed `vite.config.ts` still binds `127.0.0.1:5173` as required — I only
overrode the port in a local (git-ignored) `.claude/launch.json` for preview.

## Views → data source

| View | Route | Endpoint (`VITE_GOLAVO_API` set) | Mock fallback (default) |
| --- | --- | --- | --- |
| Matchday list | `#/` | `GET {base}/api/v1/forecasts` | `src/mocks/forecasts/*.json` |
| Forecast detail / After the whistle | `#/forecast/{id}` | `GET {base}/api/v1/forecasts/{id}` | same, by id |
| Evaluation summary | `#/eval` | `GET {base}/api/v1/eval/summary` | `src/mocks/eval-summary.json` |

`src/lib/api.ts` chooses live vs mock from `import.meta.env.VITE_GOLAVO_API` and validates
every response against the contract shape (schema_version, `fa_` id prefix, 64-hex payload
sha, probs sum to 1 ± 0.001). The list endpoint is parsed defensively (accepts a bare array
or a `{forecasts:[…]}` envelope). No other endpoints are assumed.

## Mock inventory (`src/mocks/`)

9 `ForecastArtifact` JSONs (men's international, WC2026 / Euro-era; plausible-but-illustrative
values; all validate against the contract; probs sum to 1). Hashes are real sha256 of stable
labels (reproducible) generated by a throwaway script — the committed artifacts are static JSON.

| id | fixture | status | horizon | covers |
| --- | --- | --- | --- | --- |
| `fa_8aea566bea4392f6` | Spain v Germany | sealed | T-72h | sealed / awaiting kickoff |
| `fa_7481364c89777a2a` | Argentina v England | sealed | T-24h | sealed, high uncertainty |
| `fa_2094ddf078fd973a` | France v Morocco | scored | T-24h | **win** — 2–0, dixon_coles, log loss 0.709 (the brief's example) |
| `fa_ab7bc3da29f835d6` | Netherlands v Brazil | scored | T-24h | **draw** — 1–1, poisson_independent |
| `fa_c940742dfd8ee148` | Portugal v Japan | scored | T-24h | **upset** — 0–1 away, elo_ordlogit |
| `fa_ee868591f4efe082` | Jordan v Uzbekistan | abstained | T-72h | insufficient matches; probs null |
| `fa_b1af0c2e4b125389` | Belgium v Uruguay | voided | T-24h | excluded from scoring |
| `fa_1e7e72b97f5ef903` | Mexico v Croatia (v1) | sealed | T-72h | **superseded** original; non-neutral (host nation) |
| `fa_435de0b96a8f237b` | Mexico v Croatia (v2) | scored | T-24h | **supersedes** v1; 2–1; re-sealed at T-24h |

1 `EvalSummary` JSON: 3 folds (**WC2022** n=64, **EURO2024** n=51, **WC2026** n=40) × 5 models
(climatological, elo_ordlogit, poisson_independent, dixon_coles, bivariate_poisson) with plausible
log loss / Brier / ECE / RPS. Reliability bins (9 each) present for WC2022 & WC2026; **EURO2024
intentionally omits `reliability_bins`** so the diagram's partial/empty state is reachable from the
UI alone.

The Mexico v Croatia pair also exercises the **non-neutral venue** case (host nation → the
neutral mark is absent and the header shows "Home advantage · Mexico").

## Screenshots (`ui/screenshots/`)

| File | What |
| --- | --- |
| `01-matchday-dark.png` | Matchday list, all 9 artifacts + status chips (dark) |
| `02-forecast-scored-dark.png` | France v Morocco — after-the-whistle + seal stamp + provenance |
| `03-forecast-sealed-dark.png` | Spain v Germany — sealed / awaiting kickoff |
| `04-abstained-dark.png` | Jordan v Uzbekistan — abstained, reason prominent, no probs |
| `05-superseded-dark.png` | Mexico v Croatia v1 — "Superseded" banner + preserved seal |
| `06-voided-dark.png` | Belgium v Uruguay — voided, excluded from scoring |
| `07-eval-dark.png` | Evaluation — 3 fold tables + reliability diagram |
| `08-matchday-light.png` | Matchday list in the light theme |
| `09-forecast-mobile-dark.png` | Scored detail at 390 px (single-column) |

## Accessibility verification

- **Contrast (WCAG 2.2 AA):** measured with a luminance script in-page. Body text 15.2:1;
  muted 7.35:1; dim 4.7:1. Status chips (small bold) pass in both themes — dark 4.77–7.02:1,
  light 4.98–5.83:1 (light-theme chip text was darkened to clear 4.5:1).
- **Keyboard / focus:** all interactive elements are native (`a`, `button`, `select`); a
  visible 3px gold focus ring (`:focus-visible`) is applied; a skip-link jumps to `#main`.
- **Structure:** single `h1` per view, no heading-level skips; `header`/`nav`/`main`/`footer`
  landmarks; tables use scoped `th`; the reliability `svg` has an `aria-label` summary;
  the probability bar exposes an accessible text summary and hides its decorative segments.
- **Reduced motion:** a global `prefers-reduced-motion: reduce` rule neutralises transitions
  and the skeleton shimmer; the brand lockup SVG carries its own reduced-motion fallback. (The
  handoff screenshots were captured with reduced-motion emulated.)
- **Responsive:** no horizontal page scroll at 375 px on any view; the wide 5-column eval table
  scrolls inside its own container.

## Contract gaps — notes for Codex (NOT changed here)

Per the brief, I did not touch the canonical contract. These are shapes a view wanted that
v0.1.0 cannot currently express; each was worked around read-only without inventing fields:

1. **No machine-readable void reason.** `status: "voided"` has no counterpart to abstained's
   `abstain_reason` (nor a `voided_at_utc`). The UI shows a generic "excluded from scoring"
   banner. Consider an optional `void_reason: string | null` (+ `voided_at_utc`).
2. **Supersession is backward-only.** An artifact carries `supersedes` (points at the older
   seal) but nothing points forward. The UI derives "superseded by …" by scanning the loaded
   list — fine while `/forecasts` returns whole chains, but it would break under pagination or
   filtering. Consider an optional `superseded_by` (or a documented guarantee that chains are
   returned intact).
3. **`EvalSummary` models lack a `family`.** `FoldModel` has only `model_id`; the UI prettifies
   known id prefixes (`clim`/`elo`/…) for display. An explicit `family` or `display_name` would
   be more robust.
4. **List endpoint shape is unspecified.** The brief names `GET /api/v1/forecasts` but not
   whether it returns a bare array or an envelope. The UI accepts both; worth pinning in the
   contract.
5. **No "kickoff passed but unscored" signal.** A superseded seal stays `status: "sealed"` with
   a past kickoff. The UI infers "awaiting kickoff" from `kickoff_utc` vs now to avoid mislabeling
   it. Not blocking; noting for completeness.

## Notes / deviations

- Branch name differs from `lane/claude-ui` (see BASE_SHA section) — the worktree was
  pre-provisioned.
- Preview ran on port 5174 locally (5173 held by another session); committed config keeps 5173.
- A `golavo-ui` entry was added to the git-ignored `.claude/launch.json` for local preview only.
