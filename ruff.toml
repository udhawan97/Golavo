line-length = 100
target-version = "py311"
extend-exclude = ["docs-site", "ui", "**/build", "**/dist", "**/.astro"]

[lint]
select = ["E", "F", "I", "UP", "B"]

[lint.per-file-ignores]
# These scripts emit Markdown report prose; long single-line strings are intentional.
"scripts/audit_openfootball.py" = ["E501"]
"scripts/build_model_cards.py" = ["E501"]
