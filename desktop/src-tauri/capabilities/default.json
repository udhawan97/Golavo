{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core window/event/dialog permissions for the main window. The sidecar is spawned from the Rust backend, which is not gated by capabilities; the scoped shell permission below is defence in depth in case a future frontend path needs it.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "notification:allow-is-permission-granted",
    "notification:allow-request-permission",
    "notification:allow-notify",
    {
      "identifier": "shell:allow-execute",
      "allow": [
        {
          "name": "binaries/golavo-sidecar",
          "sidecar": true,
          "args": true
        }
      ]
    }
  ]
}
